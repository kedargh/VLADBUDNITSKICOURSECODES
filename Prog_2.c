#include <stdio.h>

double main()
{ double length,breadth,area;
 length=scanf("%lf",&length);
 breadth=scanf("%lf",&breadth);
 area=length*breadth;
 printf("The area of the rectangle is = %lf",area);

}
